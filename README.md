# cursor-startup-commands

ECR, VPC, and required startup commands for Cursor Cloud Agent

# cursor-startup-commands

Startup Commands for Cursor Cloud Agent working environments

## Instructions

Add the following command to Startup Commands in Cursor Cloud Agent

```shell
/bin/bash -c "$(curl -s https://raw.githubusercontent.com/riskalyze/cursor-startup-commands/refs/heads/main/cursor-startup-commands.sh)"
```
